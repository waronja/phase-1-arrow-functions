// This function divides two numbers 2000 by 100 and returns 20.

const divide = () => {
  return 2000 / 100
}
// Square arrow function takes one parameter and multiplies it times itself

const squre = (x) => {
    return x * x
}

// This function takes two numbers and adds them together and returns the result.

const add = (a,b) => {
    let a = 3;
    let b = 4;
    return a + b
}  
 // end of add